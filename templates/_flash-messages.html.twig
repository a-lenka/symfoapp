{%- for label, messages in app.flashes -%}
    {%- for message in messages -%}
        <div class="flash-container {{ label }}">
            {{- message|trans -}}
        </div>
    {%- endfor -%}
{%- endfor -%}
